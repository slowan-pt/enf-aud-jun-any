---
/**
 * Rota antiga de Aparência — movida para dentro do Editor Visual:
 * Conteúdo → Editor Visual → Páginas → Home → Aparência
 * (ver src/pages/admin/editor/home/aparencia.astro).
 *
 * Este arquivo só existe para não quebrar um favorito ou link antigo: quem
 * chegar aqui, por GET ou POST, é redirecionado para o novo lugar. Não há
 * formulário nem gravação aqui — um único formulário editando a paleta
 * global evita duas telas divergindo sobre o mesmo dado.
 */
return Astro.redirect('/admin/editor/home/aparencia', 302);
---
